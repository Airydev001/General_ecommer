<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/cart.css">
    </head>

    <body>
        
        <main>
            <% if (hasError) {%>
           <!---<% if (products.length > 0) { %>
                <ul class="cart__item-list">
                    <% products.forEach(p => { %>
                        <li class="cart__item">
                            <h1><%= p.productId.title %></h1>
                            <h2>Quantity: <%= p.quantity %></h2>
                        </li>
                    <% }) %>
                </ul>
                <%}%>-->
                <div class="centered">
                    <h2>Sorry! the payment is not sucessful</h2>
                    <a href="/" class="btn">Back to Shop page</a>

                </div>
            <%}else{%>
                <div class="centered">
                    <h2><%=message%></h2>
                    <p>The payment for the transaction was sucessful </p>
                    <p></p>
                    <a href="/" class="btn">Back to Shop page</a>
                </div>
                <%}%>
            </main>

            
  
<script src="/js/pay.js"></script>

        <%- include('../includes/end.ejs') %>